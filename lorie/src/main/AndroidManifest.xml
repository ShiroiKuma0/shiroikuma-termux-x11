<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:installLocation="internalOnly">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS" tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- shiroikuma fork: the automation data door's dataSync foreground service (contract v2 §4). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <application
        android:allowBackup="false"
        tools:ignore="DataExtractionRules,GoogleAppIndexingWarning">
        <activity
            android:name=".MainActivity"
            android:taskAffinity=".MainActivity"
            android:icon="@mipmap/lorie_ic_launcher"
            android:roundIcon="@mipmap/lorie_ic_launcher_round"
            android:label="@string/lorie_app_name"
            android:theme="@style/LorieAppTheme"
            android:launchMode="singleInstance"
            android:supportsPictureInPicture="true"
            android:configChanges="fontScale|orientation|screenSize|keyboard|keyboardHidden|layoutDirection|locale|mcc|mnc|navigation|screenLayout|touchscreen|uiMode|smallestScreenSize|density"
            android:resizeableActivity="true"
            android:windowSoftInputMode="stateHidden|adjustNothing"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <category android:name="android.intent.category.SECONDARY_HOME" />
            </intent-filter>
            <meta-data android:name="android.app.shortcuts" android:resource="@xml/lorie_shortcuts" />
        </activity>
        <activity
            android:theme="@style/Theme.AppCompat.DayNight"
            android:excludeFromRecents="true"
            android:name=".LoriePreferences"
            android:taskAffinity=".LoriePreferences"
            android:supportsPictureInPicture="false"
            android:resizeableActivity="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.APPLICATION_PREFERENCES" />
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
	    <meta-data android:name="android.allow_multiple_resumed_activities" android:value="true" />

        <service android:name=".utils.KeyInterceptor"
            android:exported="true"
            android:label="白い熊 Termux X11 KeyInterceptor"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>

            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <receiver android:name=".LoriePreferences$Receiver" android:exported="true" tools:ignore="ExportedReceiver" android:enabled="true">
            <intent-filter>
                <action android:name="com.termux.x11.CHANGE_PREFERENCE" />
            </intent-filter>
        </receiver>

        <receiver android:name=".LorieBroadcastReceiver" android:exported="true" tools:ignore="ExportedReceiver" android:enabled="true">
            <intent-filter>
                <action android:name="com.termux.x11.CmdEntryPoint.ACTION_START" />
                <action android:name="com.termux.x11.ACTION_STOP" />
                <action android:name="com.termux.x11.ACTION_PREFERENCES_CHANGED" />
                <action android:name="com.termux.x11.ACTION_CUSTOM" />
            </intent-filter>
        </receiver>

        <!-- ==== shiroikuma fork (Phase 4): the 白い熊 Termux X11 UI page + the 保存復元 automation
             contract v2. Every component below is ALSO listed in lorie-app/src/sharedUid/AndroidManifest.xml,
             which puts it in Termux's process like the rest of the app. ==== -->
        <activity
            android:name=".shiroikuma.ShiroikumaUiActivity"
            android:label="@string/shiroikuma_ui_title"
            android:theme="@style/ShiroikumaUiTheme"
            android:taskAffinity=".LoriePreferences"
            android:excludeFromRecents="true"
            android:supportsPictureInPicture="false"
            android:resizeableActivity="true"
            android:exported="true" />

        <!-- §1: headless export for the batch. No android:permission (the caller cannot hold one) —
             this receiver only ever writes where it was told to; AutomationAuth is the gate. -->
        <receiver
            android:name=".shiroikuma.automation.StateExportReceiver"
            android:exported="true"
            tools:ignore="ExportedReceiver">
            <intent-filter>
                <action android:name="com.termux.x11.action.EXPORT_STATE" />
                <action android:name="com.termux.x11.action.LIST_CATEGORIES" />
                <action android:name="com.termux.x11.action.CANCEL_EXPORT" />
            </intent-filter>
        </receiver>

        <!-- §2a: the data door. Exported with no permission, deliberately — AutomationCallers checks
             an EXACT package name, the kernel's uid and a pinned signing certificate instead. -->
        <provider
            android:name=".shiroikuma.automation.AutomationProvider"
            android:authorities="com.termux.x11.automation"
            android:exported="true"
            tools:ignore="ExportedContentProvider" />

        <service
            android:name=".shiroikuma.automation.AutomationDataService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Capability discovery, readable WITHOUT waking this app (a frozen app cannot be asked). -->
        <meta-data android:name="shiroikuma.automation.contract" android:value="2" />
        <meta-data android:name="shiroikuma.automation.format" android:value="1" />
        <meta-data android:name="shiroikuma.automation.min_format" android:value="1" />
    </application>
    <queries>
        <package android:name="com.termux" />
        <!-- shiroikuma fork (contract v2 §2a): the automation callers. Without these the reply
             broadcast's setPackage() fails SILENTLY on Android 11+. -->
        <package android:name="shiroikuma.oyokanri" />
        <package android:name="shiroikuma.jiyusagyoban" />
    </queries>
</manifest>
